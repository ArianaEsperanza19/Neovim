--INFO: Vim Chase is a plugin for Neovim that provides a simple and intuitive way to navigate and manipulate text in a file.
return {
	"axlebedev/vim-chase",
	config = function()
		-- Mapeos defaults
		-- g + u = lowercase
		-- g + U = uppercase
	end,
}
